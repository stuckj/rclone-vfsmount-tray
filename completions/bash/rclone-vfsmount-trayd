_rclone__vfsmount__trayd() {
    local i cur prev opts cmd
    COMPREPLY=()
    if [[ "${BASH_VERSINFO[0]}" -ge 4 ]]; then
        cur="$2"
    else
        cur="${COMP_WORDS[COMP_CWORD]}"
    fi
    prev="$3"
    cmd=""
    opts=""

    for i in "${COMP_WORDS[@]:0:COMP_CWORD}"
    do
        case "${cmd},${i}" in
            ",$1")
                cmd="rclone__vfsmount__trayd"
                ;;
            rclone__vfsmount__trayd,help)
                cmd="rclone__vfsmount__trayd__subcmd__help"
                ;;
            rclone__vfsmount__trayd,prepare-mount)
                cmd="rclone__vfsmount__trayd__subcmd__prepare__subcmd__mount"
                ;;
            rclone__vfsmount__trayd__subcmd__help,help)
                cmd="rclone__vfsmount__trayd__subcmd__help__subcmd__help"
                ;;
            rclone__vfsmount__trayd__subcmd__help,prepare-mount)
                cmd="rclone__vfsmount__trayd__subcmd__help__subcmd__prepare__subcmd__mount"
                ;;
            *)
                ;;
        esac
    done

    case "${cmd}" in
        rclone__vfsmount__trayd)
            opts="-h -V --config --log-level --foreground --help --version prepare-mount help"
            if [[ ${cur} == -* || ${COMP_CWORD} -eq 1 ]] ; then
                COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
                return 0
            fi
            case "${prev}" in
                --config)
                    COMPREPLY=($(compgen -f "${cur}"))
                    return 0
                    ;;
                --log-level)
                    COMPREPLY=($(compgen -f "${cur}"))
                    return 0
                    ;;
                *)
                    COMPREPLY=()
                    ;;
            esac
            COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
            return 0
            ;;
        rclone__subcmd__vfsmount__subcmd__trayd__subcmd__help)
            opts="prepare-mount help"
            if [[ ${cur} == -* || ${COMP_CWORD} -eq 2 ]] ; then
                COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
                return 0
            fi
            case "${prev}" in
                *)
                    COMPREPLY=()
                    ;;
            esac
            COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
            return 0
            ;;
        rclone__subcmd__vfsmount__subcmd__trayd__subcmd__help__subcmd__help)
            opts=""
            if [[ ${cur} == -* || ${COMP_CWORD} -eq 3 ]] ; then
                COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
                return 0
            fi
            case "${prev}" in
                *)
                    COMPREPLY=()
                    ;;
            esac
            COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
            return 0
            ;;
        rclone__subcmd__vfsmount__subcmd__trayd__subcmd__help__subcmd__prepare__subcmd__mount)
            opts=""
            if [[ ${cur} == -* || ${COMP_CWORD} -eq 3 ]] ; then
                COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
                return 0
            fi
            case "${prev}" in
                *)
                    COMPREPLY=()
                    ;;
            esac
            COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
            return 0
            ;;
        rclone__subcmd__vfsmount__subcmd__trayd__subcmd__prepare__subcmd__mount)
            opts="-h --name --help"
            if [[ ${cur} == -* || ${COMP_CWORD} -eq 2 ]] ; then
                COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
                return 0
            fi
            case "${prev}" in
                --name)
                    COMPREPLY=($(compgen -f "${cur}"))
                    return 0
                    ;;
                *)
                    COMPREPLY=()
                    ;;
            esac
            COMPREPLY=( $(compgen -W "${opts}" -- "${cur}") )
            return 0
            ;;
    esac
}

if [[ "${BASH_VERSINFO[0]}" -eq 4 && "${BASH_VERSINFO[1]}" -ge 4 || "${BASH_VERSINFO[0]}" -gt 4 ]]; then
    complete -F _rclone__vfsmount__trayd -o nosort -o bashdefault -o default rclone-vfsmount-trayd
else
    complete -F _rclone__vfsmount__trayd -o bashdefault -o default rclone-vfsmount-trayd
fi
